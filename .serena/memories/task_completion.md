# Task completion checklist
- Review edited `SKILL.md` for wording consistency, portability, and concise instructions.
- If shell scripts are edited, run `bash -n <script>`.
- For Markdown-only skill changes, verify the file content and ensure no broken references remain.
- Optionally inspect `git diff --stat` / `git diff -- skills/<skill>/SKILL.md README.md` before finishing.